# Plot configuration.
#
# `npm run import:published` reads this file, fetches your feeds, and imports
# anything not already in published/ (matched by GUID or canonical URL).

writer:
  name: Tom Critchlow

feeds:
  - name: tomcritchlow.com
    url: https://tomcritchlow.com/feed.xml
